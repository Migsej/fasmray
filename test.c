#include <raylib.h>
#include <stdio.h>

int main() {
	//DrawText("peepee", 0, 1, 2, RAYWHITE);       // Draw text (using default font)
	//DrawRectangle(100, 200, 300, 400, RAYWHITE);                        // Draw a color-filled rectangle
																																			//
	//DrawCircle(1, 2, 1.2, RAYWHITE);
	char buffer[256] = {0};
	sprintf(buffer, "%d %d", 5, 4);
}
